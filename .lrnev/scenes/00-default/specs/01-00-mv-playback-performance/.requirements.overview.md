按 DESIGN_SPECS §6.4 与 UX-10 实施：① resolveBiliPlaybackData 对 view+playurl 做 8 分钟 withMusicCache TTL 缓存（/stream 复用）；② 首页空闲预取精选 MV（save-data/reduced-motion 感知、可取消）；③ 480P 起播并在稳定后自动升到记忆画质（版本化 localStorage）；④ dashPlayer 改 Range 按需分段拉取（60s 背压预算）+ AbortController 贯穿 + 起播即返回 cleanup + 中途错误回调；⑤ MvPlayer 移除假计时器、poster+真实缓冲文案（i18n 五语言）、播放通道开关（auto/iframe/proxy）；⑥ index.html 预连接 CDN。
