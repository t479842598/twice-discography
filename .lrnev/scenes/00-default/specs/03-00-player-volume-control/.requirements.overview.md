audio store 新增 volume/muted/lastVolume 状态与 setVolume/toggleMute，twice.audio.volume.v1 持久化（非法值/存储不可用安全回退）；MiniAudioBar 新增静音按钮与 n-slider（0-100 键盘可操作）+ 百分比值 + live region（仅静音切换与滑杆释放时通知）；<audio>.volume/.muted 与 store 双向同步（唯一播放所有者）；i18n 六 key 五语言；vitest 5 例（钳制/持久化恢复/静音恢复非零音量/非法值回退）。
